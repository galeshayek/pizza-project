// src/types/enums.ts
export enum Level {
    EASY = 'easy',
    MEDIUM = 'medium',
    HARD = 'hard',
}

export enum Role {
    ADMIN = 10,
    USER = 0,
}

export enum Category {
    DOUGH = 'dough',
    SAUCE = 'sauce',
    TOPPING = 'topping',
}
